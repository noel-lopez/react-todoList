// TODO: createTask() - think about props

export const createTask = () => {

}

// TODO: getTitle(parentIndex, tasks)

export const getTitle = (parentIndex, tasks) => {

}

// TODO: getPendingTasks(parentIndex, tasks)

export const getPendingTasks = (parentIndex, tasks) => {

}

// TODO: getInProgressTasks(parentIndex, tasks)

export const getInProgressTasks = (parentIndex, tasks) => {

}

// TODO: getDoneTasks(parentIndex, tasks)

export const getDoneTasks = (parentIndex, tasks) => {

}

// TODO: changeStatus() - think about props

export const changeStatus = () => {

}

// TODO: deleteTask() - think about props

export const deleteTask = () => {

}

// TODO: goToTask() - think about props

export const goToTask = () => {

}

// TODO: goBack() - think about props

export const goBack = () => {
  
}
